import React from "react";
import {Card,Eyebrow,Icon} from "../components/common/UI";
export default function Settings({settings,setSettings}){return <main className="content"><div className="page-head"><div><Eyebrow>SYSTEM · OPERATOR CONFIGURATION</Eyebrow><h1>Settings</h1><p>Configure presentation preferences without changing the deterministic CRAI risk engine.</p></div></div><div className="settings-grid"><Card><Eyebrow>ADVISORY</Eyebrow><h2>Operator language</h2><div className="setting-options">{["English","Tamil","Hindi"].map(x=><button key={x} className={settings.language===x?"active":""} onClick={()=>setSettings({...settings,language:x})}><span>{x==="English"?"EN":x==="Tamil"?"த":"हि"}</span><div><b>{x}</b><small>Use for local field advisory output</small></div>{settings.language===x&&<strong>✓</strong>}</button>)}</div></Card><Card><Eyebrow>SYSTEM POLICY</Eyebrow><h2>Evidence discipline</h2><SettingRow label="Fresh environmental evidence" value="15 minutes"/><SettingRow label="Risk source" value="CRAI Fusion V1.5"/><SettingRow label="LLM role" value="Explanation only"/><SettingRow label="Missing evidence" value="Never treated as zero"/></Card><Card><Eyebrow>INTERFACE</Eyebrow><h2>Display preferences</h2><Toggle label="Compact dashboard" text="Keep the enterprise shell dense and focused." checked={settings.compact} onChange={v=>setSettings({...settings,compact:v})}/><Toggle label="Offline-first cues" text="Show local readiness and source labels." checked={settings.offlineCues} onChange={v=>setSettings({...settings,offlineCues:v})}/></Card></div></main>}
function SettingRow({label,value}){return <div className="setting-row"><span>{label}</span><b>{value}</b></div>}
function Toggle({label,text,checked,onChange}){return <label className="toggle-row"><div><b>{label}</b><small>{text}</small></div><input type="checkbox" checked={checked} onChange={e=>onChange(e.target.checked)}/></label>}
